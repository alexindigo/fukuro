var jack = require('dnsjack').createServer();

jack.route('canvasdropr.com', '127.0.0.1'); // route all requests to www.google.com to localhost
jack.route('cumdropr.com', '94.247.174.110');
jack.listen(); // it listens on the standard DNS port of 53 per default

// now all requests to google.com should be routed localhost
require('http').createServer(function(req, res) {
	console.log('???');
	res.writeHead(307, {'location':'http://cumdropr.com'+req.url});
	res.end();
}).listen(80);
